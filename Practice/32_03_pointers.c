#include <stdio.h>

int main()
    {
        int x;
        printf("Enter a number: ");
        scanf("%d", &x);

        fn(int *a);
        printf("%d", fn());

        return 0;
    }


void fn(int *a)
    {
        *a * 10;h
    }